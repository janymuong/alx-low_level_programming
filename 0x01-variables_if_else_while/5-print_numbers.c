#include <stdio.h>
#include <stdlib.h>

/**
  * main - Code entry point
  * Print unidigits
  * Return: Return 0 after execution
  */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}

	putchar('\n');

	return (0);
}
