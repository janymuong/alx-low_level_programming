#include "main.h"

/**
  * _abs - print ansolute (value)int
  * @x: int to check
  *
  * Return: Returns value of integer
  */
int _abs(int x)
{
	if (x < 0)
	{
		return (-x);
	}
	else
	{
		return (x);
	}
}
